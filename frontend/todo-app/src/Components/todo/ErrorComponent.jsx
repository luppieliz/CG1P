// Generic error for pages that are not found

import React from 'react';

function ErrorComponent() {
    return <div>An error occured. Contact the site admin.</div>
}

export default ErrorComponent;